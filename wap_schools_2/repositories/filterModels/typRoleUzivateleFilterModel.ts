import { BaseFilterModel } from "./baseFilterModel";

/**
 * Filter model for typ_role_uzivatele entity
 * @property IDs - filter by typ_role_uzivatele.ID
 */
export type TypRoleUzivateleFilterModel = BaseFilterModel & {
  ID?: number;
}